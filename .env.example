# MX Connect demo password.
# DO NOT commit a real value here. Set this in Vercel project settings:
#   Settings → Environment Variables → SITE_PASSWORD
#
# Suggested: a strong random string. Generate with:
#   openssl rand -base64 16
#
# Example (placeholder — replace in Vercel dashboard):
SITE_PASSWORD=changeme
