// ── Dataverse publisher prefix ────────────────────────────────────────────────
// This is the only line that changes when deploying to a different tenant.
//
//   Dev tenant (Nathan Horstmeier personal):  'cr463_'
//   IHC production tenant:                    'cr_'
//
// After changing PREFIX, run `npm run build` — no other edits required.

export const PREFIX = 'cr463_';
